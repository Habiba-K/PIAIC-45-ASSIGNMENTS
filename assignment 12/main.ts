/* printing each person’s name with a message to them. The text of each message should 
be the same, but each message should be personalized with the person’s name.*/

let names : string [] = ["Sara", "Sana", "Rida", "Aniza"];
for(let i = 0; i< names.length; i++){
    console.log(`${names[i]} likes to play cricket`);
}